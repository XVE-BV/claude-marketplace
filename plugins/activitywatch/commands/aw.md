---
description: ActivityWatch — query time per project today. No args = full customer breakdown.
---

Use the activitywatch agent. Handle: $ARGUMENTS

If no arguments: show today's time breakdown per configured customer using all available buckets (window, web, vscode, terminal). Show non-zero results only with total.
